# GoReleaser config for the vikunja-cli CLI — https://goreleaser.com
# Cuts a release on every `vX.Y.Z` tag: cross-compiled static binaries, archives,
# SHA256 checksums, a changelog, and a self-contained Claude skill bundle, all
# published to the GitHub release. See .github/workflows/release.yml.
version: 2

project_name: vikunja-cli

before:
  hooks:
    - go mod tidy
    # Build the matching binary into the vendored skill and zip it into build/.
    - bash scripts/build-skill-bundle.sh {{ .Version }}

builds:
  - id: vikunja-cli
    main: .
    binary: vikunja-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w
        -X github.com/jo-nike/vikunja-cli/cmd.Version={{ .Version }}
        -X github.com/jo-nike/vikunja-cli/cmd.Commit={{ .ShortCommit }}
        -X github.com/jo-nike/vikunja-cli/cmd.Date={{ .Date }}
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ignore:
      # Match the existing 5-platform matrix (no windows/arm64).
      - goos: windows
        goarch: arm64

archives:
  - id: vikunja-cli
    ids:
      - vikunja-cli
    name_template: vikunja-cli-{{ .Os }}-{{ .Arch }}
    formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    files:
      - README.md

checksum:
  name_template: checksums.txt
  algorithm: sha256

changelog:
  use: git
  sort: asc

release:
  # Published to GitHub — GoReleaser infers the repo from GITHUB_TOKEN and the
  # GITHUB_REPOSITORY env in CI (github.com/jo-nike/vikunja-cli).
  github:
    owner: jo-nike
    name: vikunja-cli
  # Attach the self-contained skill bundle produced by the before-hook.
  # Staged in build/ (not dist/) so --clean doesn't choke on a non-empty dist.
  extra_files:
    - glob: ./build/vikunja-cli-skill-*.zip
